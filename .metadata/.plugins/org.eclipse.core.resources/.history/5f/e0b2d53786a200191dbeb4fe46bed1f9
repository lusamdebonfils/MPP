package prob2;

public interface Polygon {
	 double[] getSides(); 
	 default double getPerimeter() {
		double sum = 0;
		double[] sides = getSides();
		for(double side : sides) {
			sum += side;
		}
		return sum;
		 
	 }
}
